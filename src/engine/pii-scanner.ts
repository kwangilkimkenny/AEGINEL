import type { PiiMatch, AeginelConfig } from './types';

// TODO: Replace with AI model-based PII detection
export function scanPii(_input: string, _config: AeginelConfig): PiiMatch[] {
  return [];
}
